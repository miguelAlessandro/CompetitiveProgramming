#include <bits/stdc++.h>

using namespace std;

int n, m;
vector<int> v;

int main(){

    int nc = 1;
    while(scanf("%d %d", &n, &m), n + m){
        v.resize(n);
        for(int i = 0; i < n; ++i)
            scanf("%d", &v[i]);
        sort(v.begin(), v.end());
        printf("CASE# %d:\n", nc++);
        for(int q, i = 0; i < m; ++i){
            scanf("%d", &q);
            int low = 0, upper = n-1;
            while(low < upper){
                int mid = (upper + low)/2;
                if(v[mid] < q) low = mid + 1;
                else upper = mid;
            }
            if(v[low] == q)
                printf("%d found at %d\n", q, low + 1);
            else
                printf("%d not found\n", q);
        }
    }

    return 0;
}
