#include <bits/stdc++.h>

using namespace::std;

int n, a;
int v[12];

int main( void ){

    while(cin >> n && n != 0)
        for(int i = 1234; i <= 98765; i++)
        {
            memset(V, 0, sizeof V);    
            if(i < 10000)
                V[0] = true;

            a = n*i;    
        }
}
